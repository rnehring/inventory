<div class="relative inline-flex items-center justify-center w-10 h-10 overflow-hidden bg-gray-100 rounded-full dark:bg-gray-600 mx-2">
    <span class="font-medium text-gray-600 dark:text-gray-300">{{ Str::convertCase(substr($slot, 0,1), 0) }}{{ Str::convertCase(substr($slot,2,1),0) }}</span>
</div>
